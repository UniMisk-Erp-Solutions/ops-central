# Copy to frontend/.env on your working PC
VITE_SUPABASE_URL=http://192.168.16.112:54331
VITE_SUPABASE_ANON_KEY=PASTE_FROM_opc_KONG_CONTAINER
VITE_API_URL=http://192.168.16.112:54331/functions/v1/main

# --- Maintenance gate (separate cloud "Maintenance Control" project) -----------
# PUBLIC values, fail-open. This static app has no build step, so the gate reads
# these at runtime via window.MC in index.html (set them through an env.js / deploy
# snippet, same idiom as OPC_ENV). VITE_STATUS_KEY is a PUBLIC anon key — .env only,
# never commit. Leave unset to keep the gate inert.
VITE_STATUS_URL=https://qknaxyagucgepawvrgto.supabase.co
VITE_STATUS_KEY=PASTE_MAINTENANCE_PUBLIC_ANON_KEY
